# 岚岚自用版：角色世界书与 Lore API 预设

## 目标

为 SillyTavern 的 Memory Manager Concurrent 增加两类互相独立的本地预设：

1. 角色世界书预设：每个预设保存一张角色卡对应的一本 Memory 世界书和一本 Lore 世界书。应用预设时，用这一对书替换插件当前导入的世界书，方便换卡。
2. Lore API 预设：保存 API 格式、URL、Key 和模型。在配置任意 Lore／总结世界书时可以直接选择并填入；填入后仍可临时修改模型，其余生成参数继续由当前 Lore 独立配置。

## 使用者与场景

- 单用户、本机自用。
- 换角色卡时切换一组 `Memory + Lore` 世界书。
- 新 Lore 首次配置时复用常用 API 连接信息，避免重复输入 URL 和 Key。

## 范围

- 在现有世界书选择弹窗中新增角色世界书预设的创建、应用、删除。
- 创建角色预设时严格要求恰好选择一本 Memory 和一本 Lore。
- 应用角色预设时检查两本书仍存在，然后替换当前已导入世界书列表。
- 只在 Lore／总结世界书配置界面显示 Lore API 预设。
- Lore API 预设支持新建、选择填入和删除；应用时填入格式、URL、Key 和模型，但不锁定模型。
- 保留原有手工选择世界书、手工填写 API、单独修改模型及重新配置流程。
- 预设随扩展设置持久化，并纳入现有配置导入／导出。

## 非目标

- 不改 Memory 索引配置流程；当前使用索引合并模式。
- 不自动修改角色卡本身，也不自动切换 SillyTavern 当前角色。
- 不锁定 Lore API 预设带出的模型，也不覆盖其他生成参数。
- 不修改项目其他功能和默认提示词。

## 数据与隐私

- API Key 只保存到 SillyTavern 扩展本地设置及项目原有的兼容性 localStorage 副本。
- 不把任何真实 URL、Key 或用户配置写入源码、构建产物或 Git 提交。
- 界面显示预设名称，不明文展示 Key。

## 完成标准

- 可以保存多组命名的 `Memory + Lore` 角色世界书预设。
- 应用预设后当前导入列表只包含该预设的两本书，并刷新世界书面板。
- 可以保存多组命名的 Lore API 预设并在 Lore 配置中一键填入格式、URL、Key 和模型。
- 应用 Lore API 预设后仍可手动换模型，其他参数不会被覆盖。
- 原有手工流程仍可用。
- 生产构建通过，并对预设校验、缺失世界书和旧配置升级进行代码级验证。

## 追加范围：Lore 历史事件双格式召回

- 历史事件提示词同时识别“宏史卷分段”格式与 `[#起始至#结束]` 流水账格式，不再依赖固定的世界书标题。
- 自动拆分同时识别 `【宏史卷分段开始：X-Y楼】...【宏史卷分段结束：X-Y楼】` 和以 `<task completed>X-Y</task completed>` 收尾的分段；兼容标签前的转义反斜杠。
- 输出统一为 `【X楼】` 或 `【X至Y楼】事件`，同一宏史卷分段内的多条日期事件不得因楼层范围相同被错误去重。
- 两种格式分别进行解析样例验证，随后重新构建并上传自用仓库。
